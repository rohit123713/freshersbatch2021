package com.training.ifaces;

public interface Function {

	//by default all the methods are public and abstract inside an interface class
	
	//all the interface function should have apply method
	
	double apply(double arg);
}
